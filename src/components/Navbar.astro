---
// src/components/Navbar.astro
const { lang: propLang } = Astro.props;

const urlPath = Astro.url.pathname;
const urlLang = urlPath.split('/').filter(Boolean).pop();
const lang = propLang || urlLang || "de"; 

const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : `${rawBase}/`;

const languages = [
  { code: "es", label: "Español", flag: "🇪🇸" },
  { code: "en", label: "English", flag: "🇺🇸" },
  { code: "de", label: "Deutsch", flag: "🇩🇪" },
  { code: "fa", label: "فارسی", flag: "🇮🇷", dir: "rtl" },
  { code: "ru", label: "Русский", flag: "🇷🇺" },
];

const activeLangCode = languages.some(l => l.code === lang) ? lang : "de";
const currentLang = languages.find((l) => l.code === activeLangCode);

const t = {
  es: { home: "Inicio", modules: "Módulos", mindmap: "Mindmap", current: "Actual" },
  en: { home: "Home", modules: "Modules", mindmap: "Mindmap", current: "Current" },
  de: { home: "Start", modules: "Module", mindmap: "Mindmap", current: "Aktiv" },
  ru: { home: "Главная", modules: "Модули", mindmap: "Карта", current: "Тек." },
  fa: { home: "صفحه اصلی", modules: "ماژول‌ها", mindmap: "نقشه ذهنی", current: "فعلی" }
};

const nav = t[activeLangCode];
---

<nav class="sticky top-0 z-[100] w-full bg-white/90 backdrop-blur-md border-b border-gray-100 shadow-sm">
  <div class="max-w-5xl mx-auto px-4">
    <div class="flex justify-between h-16 items-center">
      
      <a href={`${base}${activeLangCode}/`} class="font-black text-xl text-purple-600 tracking-tighter">
        BEBE<span class="text-gray-800">CUTE</span>
      </a>

      <div class="flex items-center gap-2 md:gap-8">
        
        <div class="flex items-center gap-3 md:gap-8">
          <a href={`${base}${activeLangCode}/#home`} class="text-[11px] md:text-sm font-bold text-gray-600 hover:text-purple-600 transition-colors uppercase tracking-tight">
            {nav.home}
          </a>
          <a href={`${base}${activeLangCode}/#portfolio`} class="text-[11px] md:text-sm font-bold text-gray-600 hover:text-purple-600 transition-colors uppercase tracking-tight">
            {nav.modules}
          </a>
          <a href={`${base}${activeLangCode}/#mindmap`} class="text-[11px] md:text-sm font-bold text-gray-600 hover:text-purple-600 transition-colors uppercase tracking-tight">
            {nav.mindmap}
          </a>
        </div>

        <div class="relative inline-block text-left">
          <button
            id="lang-menu-btn"
            class="flex items-center gap-1.5 bg-gray-50 border border-gray-200 px-2 py-1.5 md:px-4 md:py-2 rounded-xl hover:bg-gray-100 transition-all group"
          >
            <svg class="w-4 h-4 text-gray-400 group-hover:text-purple-600 transition-colors" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="2" y1="12" x2="22" y2="12"></line>
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path>
            </svg>
            <span class="text-[10px] md:text-sm font-bold text-gray-700 uppercase">{currentLang.code}</span>
          </button>

          <div id="lang-dropdown" class="hidden absolute right-0 mt-2 w-40 md:w-48 bg-white border border-gray-100 rounded-2xl shadow-xl overflow-hidden ring-1 ring-black ring-opacity-5 animate-in fade-in zoom-in-95 duration-100">
            {languages.map((l) => {
              const isActive = l.code === activeLangCode;
              return (
                <a 
                  href={`${base}${l.code}/`} 
                  class={`flex items-center justify-between px-4 py-3 text-[11px] md:text-sm transition-all border-b border-gray-50 last:border-0 ${isActive ? 'bg-purple-50 text-purple-700 font-bold' : 'text-gray-600 hover:bg-gray-50'}`}
                  dir={l.dir || 'ltr'}
                >
                  <div class="flex items-center gap-3">
                    <span class="text-[10px] font-bold opacity-30 w-4">{l.code.toUpperCase()}</span>
                    <span>{l.label}</span>
                  </div>
                  {isActive && (
                    <span class="text-[8px] bg-purple-600 text-white px-1.5 py-0.5 rounded font-black uppercase tracking-tighter">
                      {nav.current}
                    </span>
                  )}
                </a>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>

<script>
  const initNav = () => {
    const btn = document.getElementById("lang-menu-btn");
    const dropdown = document.getElementById("lang-dropdown");
    
    btn?.addEventListener("click", (e) => {
      e.stopPropagation();
      dropdown?.classList.toggle("hidden");
    });

    // Cerrar al hacer clic fuera
    document.addEventListener("click", () => {
      dropdown?.classList.add("hidden");
    });
  };

  initNav();
  document.addEventListener('astro:after-swap', initNav);
</script>